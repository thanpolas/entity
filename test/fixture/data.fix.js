/**
 * @fileOverview a fixture of data.
 */

var fix = module.exports = {};

fix.one = {
  name: 'Oh Long Johnson',
  sortby: 1,
  _isActive: true,
};

fix.two = {
  name: 'once upon a time',
  sortby: 2,
  _isActive: false,
};

fix.relOne = {
  darname: 'one',
};

fix.relTwo = {
  darname: 'two',
};

fix.relThree = {
  darname: 'three',
};
