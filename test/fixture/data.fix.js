/**
 * @fileOverview a fixture of data.
 */

var fix = module.exports = {};

fix.one = {
  name: 'Oh Long Johnson',
  isActive: true,
};

fix.two = {
  name: 'once upon a time',
  isActive: false,
};
